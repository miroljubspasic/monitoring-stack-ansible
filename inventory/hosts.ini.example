# Monitoring Stack - Inventory Template
#
# Usage:
#   1. Copy this file: cp inventory/hosts.ini.example inventory/hosts.ini
#   2. Edit hosts.ini and configure your server IP address
#   3. Run: ansible-playbook -i inventory/hosts.ini playbooks/setup-monitoring.yml
#
# Note: hosts.ini is ignored by git (contains your actual server IPs)

[monitoring_hosts]
# Add your Hetzner Cloud server here
# Example: monitoring.example.com ansible_host=1.2.3.4 ansible_user=root

# Production server - CONFIGURE THIS!
monitoring-1 ansible_host=YOUR_SERVER_IP ansible_user=root ansible_port=22

[monitoring_hosts:vars]
ansible_python_interpreter=/usr/bin/python3
# SSH key for Ansible (see .ssh/README.md for setup instructions)
ansible_ssh_private_key_file=.ssh/monitoring_key
# Automatically accept new SSH host keys on first connection (secure)
# This will still fail if host key changes later (MITM protection)
ansible_ssh_common_args='-o StrictHostKeyChecking=accept-new'
